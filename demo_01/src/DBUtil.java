import java.sql.Connection;
import java.sql.DriverManager;
import java.sql.SQLException;


public class DBUtil { 
    private connection getConnetction() throws SQLException{
        try {
            private static url = ""
            Connection conn = DriverManager.connection(url,'','');
        }
    }
}
